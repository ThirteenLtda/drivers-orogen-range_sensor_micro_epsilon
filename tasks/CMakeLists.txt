# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(range_sensor_micro_epsilonTaskLib)
ADD_LIBRARY(${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_NAME} SHARED 
    ${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_SOURCES})
add_dependencies(${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${RANGE_SENSOR_MICRO_EPSILON_TASKLIB_HEADERS}
    DESTINATION include/orocos/range_sensor_micro_epsilon)

